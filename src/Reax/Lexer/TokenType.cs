namespace Reax.Lexer;

public enum TokenType 
{
    EOF = -1,
    UNKNOW,
    END_STATEMENT,
    IDENTIFIER,
    ASSIGNMENT,
    START_PARAMETER,
    PARAMETER_SEPARATOR,
    END_PARAMETER,
    STRING_LITERAL,
    NUMBER_LITERAL,
    LET,
    COMPARISON,
    EQUALITY,
    IF,
    ELSE,
    START_BLOCK,
    END_BLOCK,
    TERM,
    FACTOR,
    ON,
    TRUE,
    FALSE,
    ARROW,
    FUNCTION,
    RETURN,
    WHEN,
    FOR,
    TO,
    WHILE,
    AND,
    OR,
    NOT,
    IMPORT,
    ACCESS,
    SCRIPT,
    MODULE,
    CONST,
    ASYNC,
    BIND,
    TYPING,
    BOOLEAN_TYPE,
    FLOAT_TYPE,
    INT_TYPE,
    LONG_TYPE,
    STRING_TYPE,
    VOID_TYPE,
} 
