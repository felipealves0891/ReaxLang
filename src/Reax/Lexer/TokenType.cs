namespace Reax.Lexer;

public enum TokenType 
{
    EOF = -1,
    UNKNOW,
    END_STATEMENT,
    IDENTIFIER,
    ASSIGNMENT,
    START_PARAMETER,
    PARAMETER_SEPARATOR,
    END_PARAMETER,
    STRING,
    NUMBER,
    LET,
    COMPARISON,
    EQUALITY,
    IF,
    ELSE,
    START_BLOCK,
    END_BLOCK,
    TERM,
    FACTOR,
    UNARY,
    ON,
    TRUE,
    FALSE,
    ARROW,
    FUNCTION,
    RETURN,
    WHEN,
    FOR,
    TO,
    WHILE,
    AND,
    OR,
    NOT,
    IMPORT,
    ACCESS,
    SCRIPT,
    MODULE,
    CONST
} 
